/* Proof comments API for this prototype.
   Installed by `npx verb-proof init`. The handler lives in the verb-proof
   package; bump that dependency to update. Do not edit the path below
   unless you also change data-api on the script tag. */
import handler from "verb-proof/function";

export default handler;
export const config = { path: "/api/proof/*" };
